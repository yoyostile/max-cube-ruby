require 'spec_helper'

describe Max::Cube do
  it 'has a version number' do
    expect(Max::Cube::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
